rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Replace this with your production storage rules once they are exported
    // into the repo.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
